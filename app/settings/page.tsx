"use client";

import { SectionStub } from "@/components/screens/SectionStub";

export default function SettingsPage() {
  return (
    <SectionStub
      title="Settings"
      icon="ti-settings"
      activeKey="more"
      blurb="Team, roles, queue routing, and workspace preferences will live here."
    />
  );
}
